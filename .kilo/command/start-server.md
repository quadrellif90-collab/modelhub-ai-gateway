# Avvia Server (headless)
Avvia solo il gateway HTTP senza interfaccia Electron.

## Quando usarlo
- Per testare le API direttamente
- Per verificare il routing e il failover
- Per debug del server

## Comando
npm run server